name: compliance check and build test

on: [push]

jobs:
  compliance:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Advance Security Policy as Code
        uses: advanced-security/policy-as-code@v2.9.1
        with:
          policy: GeekMasher/security-queries
          policy-path: $GITHUB_WORKSPACE/policies/default.yml
          token: ${{ secrets.ACCESS_TOKEN }}
          argvs: '--disable-dependabot --disable-secret-scanning --disable-code-scanning'

  build-maven:
    needs: compliance
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Build with Maven
        id: maven
        uses: it-at-m/lhm_actions/action-templates/actions/action-maven-build@b2b49a8b4344e3b5ac90dd1ee9a0c59860c41383 # v1.0.16